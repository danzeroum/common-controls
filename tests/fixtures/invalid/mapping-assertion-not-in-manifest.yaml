# Fixture INVÁLIDA — mapping referencia assertion que não existe no manifesto.
# O validador de compatibilidade deve detectar ASSERTION-NOT-IN-MANIFEST.
suite_mapping:
  suite_id: pse-suite
  contract_version: "1.0"
  source_schema: laudo-pse-1.0
  suite_version: "0.3.0"
  assertions:
    - id: PSE-DEP-NONEXISTENT-ASSERTION
      capability: security.nonexistent
      description: >-
        Assertion que não existe nem em capabilities[] nem em future_assertions[]
        do manifesto da suíte v0.3.0.
      lifecycle: planned
      blocking_eligible: false
      requires_adapter:
        source_schema: laudo-pse-1.0
        target_contract: evidence-bundle/v1
  result_policy:
    accepted_assertion_statuses:
      - passed
    rejected_assertion_statuses:
      - failed
      - skipped
      - errored
      - not_assessed

# Fixture INVÁLIDA — mapping com assertion planned mas blocking_eligible=true.
# O schema suite-mapping.schema.json deve rejeitar: blocking_eligible=true
# exige lifecycle=implemented.
suite_mapping:
  suite_id: pse-suite
  contract_version: "1.0"
  source_schema: laudo-pse-1.0
  suite_version: "0.3.0"
  assertions:
    - id: PSE-DEP-INVENTORY-MATCH
      capability: security.dependency-inventory
      description: >-
        Assertion planejada marcada erroneamente como blocking_eligible=true.
      lifecycle: planned
      blocking_eligible: true
      requires_adapter:
        source_schema: laudo-pse-1.0
        target_contract: evidence-bundle/v1
  result_policy:
    accepted_assertion_statuses:
      - passed
    rejected_assertion_statuses:
      - failed
      - skipped
      - errored
      - not_assessed
